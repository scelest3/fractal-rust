[package]
name    = "wasm-bridge"
version.workspace = true
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
arith        = { path = "../arith" }
kernel       = { path = "../kernel" }

[dev-dependencies]
wasm-bindgen-test = "0.3"

[features]
simd = []
